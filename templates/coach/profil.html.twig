{% extends 'app/base.html.twig' %}

{% block title %}Mon Profil Premium{% endblock %}

{% block body %}
<div class="premium-profile-container">

    <!-- ================= HEADER PREMIUM ================= -->

    <div class="premium-profile-card">

        <div class="avatar-section">

            <div class="avatar-wrapper">

                {% if app.user.photo %}
                    <img src="{{ asset('uploads/profiles/' ~ app.user.photo) }}" 
                         class="premium-avatar">
                {% else %}
                    <img src="https://i.pravatar.cc/300" 
                         class="premium-avatar">
                {% endif %}

                <!-- Overlay édition -->
                <label for="photoUpload" class="avatar-overlay">
                    📷
                </label>

            </div>

            <!-- Form Upload -->
            <form action="{{ path('profile_upload_photo') }}" 
                  method="POST" 
                  enctype="multipart/form-data">

                <input type="file"
                       id="photoUpload"
                       name="photo"
                       accept="image/*"
                       capture="environment"
                       hidden
                       onchange="this.form.submit()">

            </form>

        </div>

        <div class="profile-info">
            <h2>{{ app.user.prenom }} {{ app.user.nom }}</h2>
            <p class="email">{{ app.user.email }}</p>

            <a href="{{ path('profile_edit') }}" class="btn-premium-edit">
                ✏ Modifier le profil
            </a>
        </div>

    </div>

</div>
{% endblock %}